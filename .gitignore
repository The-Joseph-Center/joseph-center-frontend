node_modules/
dist/
.env
.env.*
!.env.example
*.log
*.tsbuildinfo

# Local Netlify folder
.netlify

# Deno lock file (created by netlify dev's edge-function runtime — not used by app)
deno.lock

# macOS resource forks
._*
.DS_Store

# This repo installs with pnpm. A package-lock.json committed alongside
# pnpm-lock.yaml leaves the package manager decided by Netlify detection
# rather than by the repo, and the last one drifted four months out of date.
package-lock.json
